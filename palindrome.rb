def user_input
    input #.lowercase method (so case sensitivity isn't an issue)
end

def array
   arr = [] #input gets shoved into the array
            #with the string split into individual characters
        
end

def shove
    arr << input #use proper syntax
end

puts "enter word:" #input from user
input = gets.chomp

#get the first and last array index and compare
arr.first
arr.last

if arr.first = arr.last
    
